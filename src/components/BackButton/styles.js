import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  btnContainer: {
    flex: 1,
    alignItems: 'center',
    margin: 10,
    marginTop:28,
    shadowColor: '#000',
  },
  btnIcon: {
    height: 17,
    width: 17,
    tintColor: 'white'
  }
});

export default styles;
